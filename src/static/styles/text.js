const commonStyles = {
  fontWeight: '400',
  color: '#1d2838',
};

const styleText = () => {
  return {
    textCenter: {
      textAlign: 'center',
    },
    fs14: {
      fontSize: 14,
    },
  };
};

export default styleText();
